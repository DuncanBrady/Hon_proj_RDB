# This file marks the directory as a Python package for pytest discovery.
# You can add package-level fixtures here if needed.

# Example: Uncomment to add a package-level fixture
# import pytest
#
# @pytest.fixture(scope="package", autouse=True)
# def setup_package():
#     # Setup code before any tests in this package run
#     yield
#     # Teardown code after all tests in this package run/s